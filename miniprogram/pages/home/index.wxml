<view wx:if="{{openingVisible}}" class="opening" bindtap="skipOpening">
  <view class="opening-file-id">INTERNAL ONLY · PERFORMANCE RECORD</view>
  <view class="opening-mark"><view class="opening-flame"></view></view>
  <view class="opening-brand">烛龙实验室</view>
  <view class="opening-product">绩效内测会</view>
  <view class="opening-status">
    <view>正在加载年度工作痕迹……</view>
    <view>正在连接组织……</view>
    <view>正在搜索优秀名额……</view>
  </view>
  <view class="opening-punchline"><view>员工档案未找到。</view><view>问题不大，系统一般也不看这个。</view></view>
  <view class="opening-skip">SYSTEM BOOTING · 点击跳过</view>
</view>

<view wx:else class="page-shell home">
  <view class="brand-row">
    <view class="brand-fire"></view>
    <view class="brand-name">烛龙实验室｜绩效内测会</view>
  </view>
  <view class="hero">
    <view class="eyebrow beta-label">PERFORMANCE REVIEW BETA</view>
    <view class="title">{{title}}</view>
    <view class="subtitle">{{subtitle}}</view>
    <view class="hook">
      <view>你努力了多少，和组织承认你努力了多少，</view>
      <view class="hook-emphasis">通常不是一回事。</view>
      <view class="hook-note">判定依据：一些很严谨的职场玄学</view>
    </view>
  </view>

  <view wx:if="{{versionNotice}}" class="notice status-strip">{{versionNotice}}</view>
  <view wx:if="{{pairInviteVisible}}" class="notice status-strip">
    <view class="notice-title">有人等你对口径</view>
    <view>先做完 25 题，看看你们凑一起是什么东西。</view>
  </view>

  <view class="actions">
    <block wx:if="{{hasResume}}">
      <view class="resume panel">
        <view class="resume-kicker">上次的组织流程还没放过你</view>
        <view class="resume-label">{{resumeLabel}}</view>
        <button class="primary-button" bindtap="resume">{{resumeActionLabel}}</button>
        <button class="text-button" bindtap="restart">重新开始</button>
      </view>
    </block>
    <block wx:else>
      <view class="cta-prompt">不信，但想试试<text class="prompt-arrow">↓</text></view>
      <button class="primary-button start" bindtap="start">开始打分<text class="button-spark">✦</text></button>
    </block>
  </view>

  <view class="light-tip">{{description}}</view>
  <view class="disclaimer">{{disclaimer}}</view>
  <view wx:if="{{introVisible}}" class="intro-backdrop" catchtap="closeIntro">
    <view class="intro-card panel" catchtap="noop">
      <view class="intro-kicker">BEFORE WE START</view>
      <view class="intro-title">先说好，这不是考试。</view>
      <view class="intro-copy">过去大半年里，你干过的那些活，经过项目、汇报、领导、同事、名额，以及一些自然现象以后——到了绩效季，最后会被系统算成什么样。</view>
      <view class="intro-emphasis">25题，凭第一反应。</view>
      <view class="intro-copy">哪个像你就点哪个。</view>
      <view class="intro-copy">想太久也没什么用。真到绩效会的时候，一般也没人给你这么久。</view>
      <button class="primary-button intro-button" bindtap="beginTest">来吧，算算看</button>
      <view class="intro-footer">职场娱乐测试。系统也没有参加过你们公司的绩效会，至少目前没有证据。</view>
    </view>
  </view>
</view>
