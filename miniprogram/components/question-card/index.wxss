@import "../../styles/tokens.wxss";

.question { max-width:660rpx; white-space:pre-line; color:var(--color-text); font-size:40rpx; font-weight:800; line-height:1.48; text-align:left; letter-spacing:-.5rpx; }
.options { display:flex; flex-direction:column; gap:var(--space-sm); margin-top:46rpx; padding-bottom:24rpx; }
