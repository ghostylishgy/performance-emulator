@import "../../styles/tokens.wxss";

.question { max-width:660rpx; white-space:pre-line; color:var(--color-text); font-size:39rpx; font-weight:800; line-height:1.5; text-align:left; letter-spacing:-.5rpx; }
.options { display:flex; flex-direction:column; gap:22rpx; margin-top:50rpx; padding-bottom:24rpx; }
