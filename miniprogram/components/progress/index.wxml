<view class="wrap">
  <view class="track"><view class="fill" style="transform: scaleX({{total > 0 ? current / total : 0}});"></view></view>
  <text class="count">{{current}} / {{total}}</text>
</view>
