<view class="card">
  <view class="title">{{content.title}}</view>
  <view class="paragraph {{index === 0 ? 'lead' : ''}}" wx:for="{{content.paragraphs}}" wx:key="index">{{item}}</view>
  <view class="footer">{{content.footer}}</view>
</view>
